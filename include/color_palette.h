#ifndef COLOR_PALETTE_H
#define COLOR_PALETTE_H

#include <wx/colour.h>

class color_palette
{
    public:
        color_palette();
        static const wxColour bg_color;
        static const wxColour higher_bg_color;
        static const wxColour highest_bg_color;
        static const wxColour toolbar_color;
        static const wxColour text_color;


    protected:

    private:
};

#endif // COLOR_PALETTE_H
